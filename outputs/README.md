# Outputs

- `reference/` contains the committed tables and figures reported with the
  paper.
- `generated/` is created when the analysis is rerun and is ignored by git.

Keeping recreated results separate prevents a routine replication run from
silently changing the published reference artifacts.
